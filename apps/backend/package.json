{
	"name": "@repo/backend",
	"type": "module",
	"scripts": {
		"dev": "nodemon --watch src --ext ts --exec \"clear && pnpm clean && pnpm build && node dist/server.cjs\"",
		"build": "tsc && node esbuild.config.js",
		"test": "vitest run",
		"clean": "rm -rf dist/*",
		"lint": "eslint"
	},
	"devDependencies": {
		"@repo/eslint": "workspace:^",
		"@repo/typescript": "workspace:^",
		"@repo/vitest": "workspace:^",
		"esbuild": "^0.27.3"
	},
	"dependencies": {
		"@effect/platform-node": "4.0.0-beta.10",
		"@repo/domain": "workspace:^",
		"effect": "4.0.0-beta.10"
	}
}
